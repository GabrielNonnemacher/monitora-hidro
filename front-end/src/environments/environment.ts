import { Environment } from '../app/shared/environment/environment.type';

export const environment: Environment = {
  production: false,
  apiUrl: 'https://monitora-hidro.onrender.com',
  apiKey: 'test-api-key',
};
